_G.CreateIndexClass = function(aux, vpx)
	print("IndexClassFunctionData: " .. aux .. " - " .. vpx)
	-- InfoTypeData Loaded
	local Part = Instance.new("Part", game.Workspace)
	Part.Transparency = 0.5
	Part.Color = Color3.fromRGB(0, 0, 0)
	local IndexClassPartLocal = Instance.new("Explosion")
	local IndexForPart = Instance.new(IndexClassPartLocal, game.Workspace)
end

